	  Write a program to find the second smallest element in a 1D array.
	   int [] arr= {4,967,9,74,6,75,97,98};
		  int min=arr[0];
		  int secmax=Integer.MAX_VALUE;
		  for (int i = 0; i < arr.length; i++) {
			  if(arr[i]<min) {
				  secmax = min;
				  min= arr[i];
			  }
			  else if(arr[i]<secmax && arr[i] != min) {
				  secmax=arr[i];
			  }
		  }
		  System.out.println(secmax);
