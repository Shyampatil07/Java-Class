	   Write a program to count frequency of each element in a 1D array.
	   int [] arr= {4,967,9,74,6,75,97,98,9};
	
	   for (int i = 0; i < arr.length; i++) {
		   int count=1;
         for (int j = i + 1; j < arr.length; j++) {   
	
        	 if(arr[i]==arr[j]) {
        		 count += 1;
        	 }
         }
         System.out.println("index of "+i+ " elememt "+arr[i]+" count is "+count);
	   }
